import math
PI= math.pi

def circlr_area(r):
    return(PI*r*r)

def circlr_perimeter(r):
    return(2*r*PI)

def rec_area(l,b):
    return(l*b)

def rec_perimeter(l,b):
    return(2*(l+b))

def sq_area(l):
    return(l*l)

def sq_perimeter(l):
    return(4*l)

polygon=''
while(polygon!='exit'):

    polygon= input("enter the polygon (circlr/square/rectangle/exit): ")
    property= ''

    if(polygon=='circle'):
        r=float(input("enter the radius: "))
        while(property!='back'):
            property= input("enter the property(area/circumference/back): ")
            if(property=='area'):
                print('area of circle is: ',circlr_area())

            elif(property=='circumference'):
                print("circumference of circle: ",circlr_perimeter())

            elif(property=='back'):
                break

            else:
                print("enter correct input")

    elif(polygon=='rectangle'):
        l=int(input("enter the length: "))
        b=int(input("enter the breadth: "))
        while(property!='back'):
            property= input("enter the property(area/perimeter/back): ")
            if(property=='area'):
                print("area of rectangle: ",rec_area())

            elif(property=='perimeter'):
                print("perimeter of rectangle: " ,rec_perimeter())

            elif(property=='back'):
                break

            else:
                print("enter correct input")


    elif(polygon=='square'):
        l=int(input("enter the length of side: "))
        while(property!='back'):
            property= input("enter the property(area/perimeter/back): ")
            if(property=='area'):
                print("area of square: ",sq_area())

            elif(property=='perimeter'):
                print("perimeter of square: " ,sq_perimeter())

            elif(property=='back'):
                break

            else:
                print("enter correct input")

    else:
        print("enter correct input")


