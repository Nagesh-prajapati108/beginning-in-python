def rsearch(l, k) :
  beg=0
  end=len(l)-1
  if(begin == end) :
    if(l[begin] == k):
      return 1
    
    else :
      return 0
  
  if(end-begin == 1):
    if(l[begin] == k or l[end] == k):
       return 1
  
    else :
      return 0
  
  if(end-begin>1):
    mid=(end+begin)//2
    if(l[mid]>k):
      end=mid-1
  
    if(l[mid]<k):
      begin=mid+1
  
    if(l[mid] == k):
      return 1
  
  if(end-begin<0):
    return 0
  
  return rsearch(l,k,begin, end)
